<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white ">
        <div class="w-full flex justify-end">
            <a href="/logout" class="bg-red-500 p-2 rounded-md border-2 border-red-500">Logout</a>
        </div>
        
        <h1 class="text-4xl mb-5">Hello , <%= user.name %></h1>
        <h5 class="mb-3">Create a new Post </h5>
        <form action="/post" method="post">
            <textarea class="mb-3 w-1/3 resize-none bg-transparent border-2 border-zinc-700 rounded-md p-3 outline-none block" name="content" placeholder="What's in your mind" ></textarea>
            <input type="submit" value="Create Post" class="bg-blue-500 border-2 border-blue-400 rounded-md p-2 block">
        </form>

        <div class="Posts">
            <h3 class="text-2xl mt-10 mb-3 text-zinc-400">Your Posts...</h3>
            <div class="post-container">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 border-2 border-zinc-700 p-2 rounded-md bg-zinc-800 mb-2">
                        <h4 class="text-blue-400 mb-2">@<%= user.username %></h4>
                        <p><%= post.content %></p>
                        <small class="text-small"><%= post.likes.length %>Likes</small>
                        <div class="btns flex gap-7 mt-3 ">
                            <a href="/like/<%= post._id %>" class="text-blue-400">
                                <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                            </a>
                            <a href="/edit/<%=post._id%>" class="text-zinc-500">Edit</a>
                        </div>
                    </div>

                <% }) %>               
                
            </div>
        </div>
        
    </div>
</body>
</html>